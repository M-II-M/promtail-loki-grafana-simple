apiVersion: 1

providers:
  - name: 'Base dashboard'
    orgId: 1
    folder: 'Base dashboard'
    folderUid: ''
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
